[package]
name = "lokalit"
version = "0.1.0"
edition = "2024"

[dependencies]
caps = "0.5.6"
libseccomp = "0.4.0"
nix = {version = "0.31.3", features = ["sched", "process", "signal", "hostname", "mount", "fs"]}
